---
name: pytest-single-file
description: Create tests (pytests) for individual files.
---

# Pytest Single File

Create the tests (pytest) that ensure that the code works properly based on the project or feature definition only for the files the user pass as argument.
If the repository has no unit tests structure, create it.
